/**
 * Checkout layout — full-page, no sidebar.
 * Uses dashboard layout for auth but overrides visual shell.
 */
export default function CheckoutLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
